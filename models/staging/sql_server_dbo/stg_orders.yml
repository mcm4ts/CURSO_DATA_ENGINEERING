version: 2
models:
  - name: stg_orders
    columns:
      - name: order_id
        tests: [not_null, unique]
      - name: status
        tests:
          - accepted_values:
              arguments:
                values: ['created', 'processing', 'delivered', 'returned', 'canceled'] # ajusta a tu catálogo real
      - name: order_total
        tests:
          - not_null